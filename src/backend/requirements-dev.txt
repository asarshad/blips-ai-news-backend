-r requirements.lock

# Test runner + coverage
pytest==9.0.2
pytest-cov==7.0.0
pytest-xdist==3.8.0

# Determinism + safety
freezegun==1.5.5
pytest-socket==0.7.0

# HTTP mocking for requests
requests-mock==1.12.1

# Redis fakes for unit tests
fakeredis==2.34.1

# Integration infra (Docker)
testcontainers[postgres,redis]==4.14.2

# Lint (CI) + pre-commit hooks
ruff==0.15.7
pre-commit==4.5.1
